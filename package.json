{
    "name": "salesforce-scripts",
    "version": "1.0.0",
    "scripts": {
        "log:delete": "node ./scripts/deleteApexLogs.js",
        "debugMode:enable": "node ./scripts/debugMode.js -e",
        "debugMode:disable": "node ./scripts/debugMode.js -d",
        "debugSetup:save": "node ./scripts/createTraceFlags.js -s",
        "debugSetup:default": "node ./scripts/createTraceFlags.js -d"
    },
    "devDependencies": {
        "jsforce": "^1.9.3",
        "minimist": "^1.2.0",
        "prompts": "^2.3.0"
    }
}
